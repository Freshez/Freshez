import React from 'react';
import PropTypes from 'prop-types';

const VendorProfileAbout = ({
  vendorprofile: {
    bio,
    user: { name },
  },
}) => {
  return <div></div>;
};

VendorProfileAbout.propTypes = {};

export default VendorProfileAbout;
